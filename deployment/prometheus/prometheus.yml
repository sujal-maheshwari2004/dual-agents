global:
  scrape_interval: 15s

scrape_configs:
  - job_name: dual-agents-backend
    static_configs:
      - targets:
          - dual-agents-backend-lmzcevoixa-uc.a.run.app
    metrics_path: /metrics
    scheme: https
    tls_config:
      insecure_skip_verify: false   # Cloud Run has valid certs, keep this false
    honor_labels: true