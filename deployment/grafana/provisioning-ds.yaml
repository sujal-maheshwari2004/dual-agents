apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # PROMETHEUS_URL is set as a Cloud Run env var, e.g. https://backend-xxxx.run.app
    url: ${PROMETHEUS_URL}
    isDefault: true
    editable: false